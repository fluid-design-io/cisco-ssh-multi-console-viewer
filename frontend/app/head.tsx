import { HeadDefaultTags } from '../lib/HeadDefaultTags';

export default function Head() {
  return (
    <>
      <title>Cisco IOS Manager</title>
      <meta name='description' content='Multi ssh console viewer from a single command line' />
      <HeadDefaultTags />
    </>
  );
}
